<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Форс-удаляем чувствительное разрешение, которое приносит зависимость -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        tools:node="remove" />

    <!-- Видимость пакетов/интентов для открытия ссылок и приоритета клиентов -->
    <queries>
        <!-- Открытие любых http/https ссылок -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>

        <!-- Точечные клиенты (если задаёшь setPackage(...)) -->
        <package android:name="org.telegram.messenger"/>
        <package android:name="com.google.android.youtube"/>
        <package android:name="com.instagram.android"/>
        <package android:name="com.zhiliaoapp.musically"/> <!-- TikTok -->
    </queries>

    <!-- БАЗОВЫЕ разрешения приложения -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".application.VoltHomeApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoltHome"
        android:usesCleartextTraffic="false"
        tools:replace="android:allowBackup"
        tools:targetApi="31">

        <!-- Client ID для Яндекс ID SDK 3.1.3 -->
        <meta-data
            android:name="com.yandex.auth.CLIENT_ID"
            android:value="${YANDEX_CLIENT_ID}" />

        <meta-data
            android:name="console_app_id_value"
            android:value="@string/rustore_console_app_id" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VoltHome"
            android:launchMode="singleTop">

            <!-- Лаунчер -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ✅ Deeplink callback для RuStore Pay SDK -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/rustore_pay_deeplink_scheme" />
            </intent-filter>

            <!-- ✅ Связка схемы с Pay SDK -->
            <meta-data
                android:name="sdk_pay_scheme_value"
                android:value="@string/rustore_pay_deeplink_scheme" />
        </activity>

        <activity
            android:name=".ui.DocumentActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>
</manifest>
